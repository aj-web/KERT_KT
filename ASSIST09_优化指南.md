# ASSIST09 图构建优化指南

## 📊 问题诊断

### 当前状态（top_k=10）

```
图结构：
  1阶邻居: 12.15  ← 太多
  2阶邻居: 63.76  ← 过度密集
  非零元素: 61.71% ← 极度密集

训练表现：
  Epoch 2: Val AUC 0.8343 (峰值)
  Epoch 7: Val AUC 0.8089 (严重过拟合，下降3%)
  
问题：
  ❌ 图太密集 → 过拟合训练集的学习路径
  ❌ Loss下降但AUC下降 → 典型过拟合特征
```

---

## 🔧 优化方案

### 修改1：降低图密度

**文件**: `data/process_assist09.py` 第335行

```python
# 修改前
top_k_neighbors=10,  # 每个知识点最多10个邻居

# 修改后
top_k_neighbors=6,   # 每个知识点最多6个邻居（小数据集，防止过拟合）
```

**预期效果**：
- 1阶邻居: 12.15 → **~6-7**
- 2阶邻居: 63.76 → **~30-40**
- 非零元素: 61.71% → **~30%**

---

### 修改2：增强正则化

**文件**: `experiments/run_experiment.py` 第328-332行

```python
# 修改前
'dropout': 0.28,      # 原dropout
'l2_lambda': 1e-5,    # 原L2正则化

# 修改后
'dropout': 0.35,      # 增大dropout防止密集图过拟合（0.28→0.35）
'l2_lambda': 5e-5,    # 增强L2正则化（1e-5→5e-5）
```

**预期效果**：
- 更强的正则化
- 减轻过拟合
- 提升泛化能力

---

## 📈 预期改善

### 图结构

| 指标 | 修改前 (k=10) | 修改后 (k=6) | 改善 |
|------|--------------|-------------|------|
| 1阶邻居 | 12.15 | **~6.5** | **-46%** |
| 2阶邻居 | 63.76 | **~35** | **-45%** |
| 图密度 | 61.71% | **~30%** | **-51%** |

### 训练性能

| 指标 | 修改前 | 修改后 (预期) | 改善 |
|------|-------|-------------|------|
| **Val AUC (峰值)** | 0.8343 | **0.840-0.850** | **+0.7-1.7%** |
| **过拟合程度** | 严重 (-3%) | **轻微 (-1%)** | **改善2%** |
| **收敛速度** | 2 epochs | **3-5 epochs** | 图模块开始工作 |
| **Test AUC** | ? | **0.845-0.855** | 预期提升 |

---

## 🚀 执行步骤

### 步骤1：重新处理ASSIST09数据

```bash
cd E:\IDAEWOREKSPACE\demo

# 重新处理数据（使用新的top_k=6）
python data/process_assist09.py
```

**预期输出**：
```
[混合图统计]:
  平均邻居数: ~6.5 (不含自环)  ← 应该接近6
  孤立节点数: <20 (15%)          ← 可接受
```

---

### 步骤2：合并数据集

```bash
python data/merge_datasets.py
```

---

### 步骤3：运行实验

```bash
python experiments/run_experiment.py --dataset assist09 --n_runs 3
```

---

## 🔍 验证要点

### 图初始化阶段

```
✅ 期望看到：
  1阶邻居平均数量: ~6.5
  2阶邻居平均数量: ~35
  非零元素比例: ~0.30

❌ 如果看到：
  1阶邻居 > 10  → 仍然太密集
  2阶邻居 > 50  → 需要进一步降低
```

### 训练阶段（前5个epoch）

```
✅ 正常情况：
  Epoch 1: Val AUC 0.82
  Epoch 2: Val AUC 0.83
  Epoch 3: Val AUC 0.84 (峰值)
  Epoch 4: Val AUC 0.84 (稳定)
  Epoch 5: Val AUC 0.83 (轻微下降)

目标：
  - 峰值AUC > 0.84
  - 过拟合 < 1%
  - 收敛稍慢（3-5 epochs）说明图模块在工作
```

---

## 📊 理论依据

### ASSIST09的特殊性

```
数据规模：小
  - 123个知识点
  - 23万训练样本
  
任务特点：简单
  - AUC 0.83+ 说明任务不难
  - LSTM已经能很好学习
  
最佳策略：
  - 适度的图密度（6邻居）
  - 避免过度拟合训练集模式
```

### 图密度的黄金法则

| 知识点数量 | 推荐邻居数 | 理由 |
|----------|-----------|------|
| < 200 | **6-8** | 避免过拟合 |
| 200-500 | **8-10** | 平衡性能和泛化 |
| > 500 | **8-12** | 充分利用数据 |

**ASSIST09 (123节点) → 推荐6邻居**

---

## 🎯 成功标准

### 最低标准（可接受）

- ✅ 图密度适中（~30%）
- ✅ Val AUC > 0.835（不低于旧版）
- ✅ 过拟合 < 2%
- ✅ Test AUC > 0.835

### 理想标准（优秀）

- ✅ 图密度合理（25-35%）
- ✅ Val AUC > 0.840（提升0.7%）
- ✅ 过拟合 < 1%
- ✅ Test AUC > 0.845（提升1%）
- ✅ 收敛3-5 epochs（图模块工作）

---

## 📝 对比实验

### 三种图构建方法对比

| 方法 | 邻居数 | Val AUC | 过拟合 | 说明 |
|------|-------|---------|-------|------|
| Q-matrix (旧) | 1.06 | 0.8367 | 小 | 图太稀疏 |
| 转移关系 (k=10) | 12.15 | 0.8343 | **严重** | 图太密集 |
| 转移关系 (k=6) | ~6.5 | **0.84+** (预期) | 小 | **理想** |

---

## 🔬 如果效果仍不理想

### 情况A：仍然过拟合

**症状**：Val AUC 在2-3 epochs后持续下降

**解决**：
```python
# 进一步降低图密度
top_k_neighbors=5,  # 6 → 5

# 或增强正则化
'dropout': 0.4,     # 0.35 → 0.4
```

---

### 情况B：性能下降

**症状**：Val AUC < 0.835（低于旧版）

**解决**：
```python
# 适度增加图密度
top_k_neighbors=7,  # 6 → 7

# 或降低正则化
'dropout': 0.3,     # 0.35 → 0.3
```

---

### 情况C：混合方法

如果单一方法效果不佳，考虑混合：

```python
# 结合Q-matrix和转移关系
graph = 0.3 * Q_matrix + 0.7 * Transition
```

**优势**：
- Q-matrix提供稳定的结构信息
- 转移关系提供动态的学习路径
- 平衡两者，避免过度依赖单一方法

---

## 📊 实验日志模板

### 图结构统计

```
1阶邻居: _____
2阶邻居: _____
非零元素: _____
孤立节点: _____
```

### 训练性能

```
Epoch 1: Val AUC: _____, Loss: _____
Epoch 2: Val AUC: _____, Loss: _____
Epoch 3: Val AUC: _____, Loss: _____
...
峰值: Epoch ___, Val AUC: _____
Test AUC: _____
```

### 对比

```
旧图 (Q-matrix):   Val AUC 0.8367, Test AUC 0.8345
新图 (k=10):       Val AUC 0.8343, Test AUC _____
优化后 (k=6):      Val AUC _____, Test AUC _____

改善: _____ %
```

---

## ✅ 检查清单

开始前：
- [x] 修改 process_assist09.py (top_k=6)
- [x] 修改 run_experiment.py (dropout=0.35, l2=5e-5)
- [ ] 重新处理数据
- [ ] 合并数据集

运行中：
- [ ] 验证图结构统计
- [ ] 监控前5个epoch
- [ ] 记录峰值性能

完成后：
- [ ] 对比三种方法
- [ ] 分析是否达到目标
- [ ] 准备实验报告

---

## 🎯 预期结论

### 假设验证

**假设1**：适度的图密度（6邻居）比极度稀疏（1.06邻居）或过度密集（12.15邻居）更好

**预期**：✅ 验证成功
- Val AUC: 0.8367 (旧) → 0.840+ (新)
- 过拟合: 减轻
- 泛化能力: 提升

**假设2**：小数据集需要更保守的图密度参数

**预期**：✅ 验证成功
- ASSIST09 (123节点) → 6邻居最优
- Junyi (713节点) → 8邻居最优

---

**祝优化成功！预计性能提升到0.84+** 🎯

