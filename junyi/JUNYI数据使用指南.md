# Junyi 数据集使用指南

## 📚 数据集概述

**Junyi Academy** 是台湾地区的在线学习平台，本数据集包含数学练习的学习记录。

### 数据规模
```
原始记录：25,925,992 条（约 2600 万）
学生数量：247,606 人
练习题目：721 个 exercise
答题正确率：82.79%（较高水平）
平均序列长度：104.71 次交互
```

---

## 🔧 数据处理策略

### **当前配置：Exercise 作为 Concept**

```python
# 映射策略（已配置在 download_with_edudata.py）
'junyi': {
    'user_id': 'student_id',          # 学生ID
    'exercise': 'question_id',        # 练习作为题目
    'exercise': 'concept_id',         # 同时作为知识点（细粒度）
    'correct': 'correct',             # 答题正确性
    'time_done': 'timestamp',         # 时间戳
}
```

**核心思想**：
- ✅ 每个 `exercise` 既是一道题目，也是一个知识点概念
- ✅ 得到 **721 个 concept**（比使用 topic 的 39 个更细粒度）
- ✅ 更接近论文表4.1的 **835 个知识点**

### 数据层次结构

```
┌────────────────────────────────────────┐
│  Level 1: Area (领域)                  │  例如：代数、几何
│            ↓                           │
│  Level 2: Topic (主题) - 39个          │  例如：一元一次方程
│            ↓                           │
│  Level 3: Exercise (练习) - 721个 ⭐   │  例如：solving_linear_equations_1
│            ↓                           │  【当前使用此级别】
│  Level 4: Problem (具体题目实例)        │  同一练习的不同变体
└────────────────────────────────────────┘
```

**对比其他数据集**：
| 数据集 | 知识点粒度 | 知识点数 |
|--------|-----------|---------|
| ASSIST09 | Skill | 124 |
| ASSIST17 | Skill | 102 |
| **Junyi** | **Exercise** | **721** ⭐ |

---

## 🚀 快速开始

### 步骤1：下载数据（如已完成可跳过）

```bash
# 使用 EduData 下载
python data/download_with_edudata.py --dataset junyi

# 说明：会下载 junyi.rar 并自动解压到 junyi/ 文件夹
```

**文件列表**：
```
junyi/
├── junyi_ProblemLog_original.csv     (2.5GB) ⭐ 主数据文件
├── junyi_Exercise_table.csv          (137KB)   练习元信息
├── relationship_annotation_*.csv      (73KB+)  关系标注
├── junyi_ProblemLog_for_PSLC.txt     (9.1GB)   PSLC格式（可选）
└── README.txt                         (5.6KB)   数据说明
```

### 步骤2：转换数据格式

```bash
# 转换为项目标准格式
python data/download_with_edudata.py --dataset junyi --skip-download

# 输出：data/junyi.csv
```

**预期输出**：
```
============================================================
[OK] 转换完成
============================================================
输出文件: ./data/junyi.csv
数据形状: (25925984, 5)

数据集统计:
  学生数: 247,606
  题目数: 721
  知识点数: 721        ⭐ exercise作为concept
  答题记录数: 25,925,984
  平均正确率: 0.8279
```

### 步骤3：生成实验数据

```bash
# 处理为实验所需的 pkl 格式
python data/process_downloaded_data.py --datasets junyi --output ./data/processed_datasets.pkl
```

**处理内容**：
- ✅ 按学生划分数据集（7:1:2）
- ✅ 构建概念图（concept_graph）
- ✅ 生成 Q 矩阵（question-concept映射）

### 步骤4：运行实验

```bash
# 快速测试（1次运行，验证代码）
python experiments/run_experiment.py --dataset junyi --n_runs 1

# 完整实验（5次运行，论文标准）
python experiments/run_experiment.py --dataset junyi --n_runs 5
```

---

## ⚙️ 配置说明

### 模型超参数（自动配置）

```python
# experiments/run_experiment.py 中的 junyi 配置
'junyi': {
    # 模型结构
    'embed_dim': 256,       # 知识点嵌入维度（论文表4.4）
    'hidden_dim': 512,      # LSTM隐藏层维度
    'n_layers': 3,          # 图传播层数（知识点多，用3层）
    
    # 三支决策阈值
    'alpha': 0.65,          # 接受阈值（略低于ASSIST09）
    'beta': 0.35,           # 拒绝阈值
    'lambda_decay': 0.1,    # 负域衰减系数
    
    # 训练参数
    'batch_size': 64,       # 大数据集用大batch
    'max_seq_len': 200,     # 序列截断长度（均值105）
    'dropout': 0.3,         # Dropout率
    'l2_lambda': 1e-5,      # L2正则化
    
    # 学习率
    'lr_kt_pretrain': 0.001,   # 预训练学习率
    'lr_kt_finetune': 0.0005,  # 微调学习率
    'lr_rl': 1e-4,             # RL学习率
    
    # 训练策略
    'n_epochs': 100,
    'patience': 10,         # Early stopping
}
```

### 资源需求

| 资源 | 最低配置 | 推荐配置 |
|------|---------|---------|
| **GPU** | 8GB VRAM | 16GB VRAM |
| **内存** | 16GB | 32GB |
| **磁盘** | 15GB | 20GB |
| **训练时间/epoch** | 20-40分钟 | 10-20分钟 |

---

## 📊 预期实验结果

### 性能预期

| 指标 | 预期范围 | 说明 |
|------|---------|------|
| **Val AUC** | 0.82-0.87 | 比ASSIST09略好（数据量大） |
| **Val ACC** | 0.78-0.83 | 与正确率相关 |
| **训练稳定性** | 好 | 大数据集缓解过拟合 |
| **收敛速度** | epoch 5-15 | 比ASSIST09慢（数据多） |

### 与 ASSIST09 对比

```
┌─────────────────┬────────────┬────────────┐
│     指标        │  ASSIST09  │   Junyi    │
├─────────────────┼────────────┼────────────┤
│ 数据规模        │   326K     │   26M ⭐   │
│ 知识点数        │   124      │   721 ⭐   │
│ 过拟合风险      │   高       │   低 ✓     │
│ 训练时间/epoch  │   2-5分钟  │   20-40分钟│
│ 预期最佳AUC     │   0.81     │   0.85 ⭐  │
└─────────────────┴────────────┴────────────┘
```

---

## 🔧 常见问题与解决

### Q1: 内存不足（OOM）

**症状**：训练时出现 `CUDA out of memory` 或系统内存不足

**解决方案**：
```python
# 修改 experiments/run_experiment.py
'junyi': {
    'batch_size': 32,      # 64 → 32
    'max_seq_len': 100,    # 200 → 100
    'embed_dim': 128,      # 256 → 128
    'hidden_dim': 256,     # 512 → 256
}
```

### Q2: 训练太慢

**解决方案1：使用数据采样**
```python
# 在 process_downloaded_data.py 的 process_dataset 函数中添加
df = df.sample(frac=0.1, random_state=42)  # 只用10%数据
print(f"  [采样] 使用 {len(df):,} 条记录（10%）")
```

**解决方案2：减少知识点数（降级到topic）**
```python
# 修改 download_with_edudata.py 的 junyi 映射
'junyi': {
    'user_id': 'student_id',
    'exercise': 'question_id',
    'topic': 'concept_id',      # 改回 topic（39个）
    'correct': 'correct',
    'time_done': 'timestamp',
}
```

### Q3: 过拟合严重

**诊断**：验证AUC在前几个epoch达到峰值后快速下降

**解决方案**：参考 `参数优化记录.md` 中 ASSIST09 的优化经验
```python
# 增强正则化
'dropout': 0.5,           # 0.3 → 0.5
'l2_lambda': 2e-4,        # 1e-5 → 2e-4
'lr_kt_pretrain': 0.0005, # 0.001 → 0.0005
```

---

## 📈 实验监控

### 关键指标

运行实验时，关注以下指标：

**✅ 正常训练**：
```
Epoch 1: Loss=0.45, Val AUC=0.78, Val ACC=0.74
Epoch 2: Loss=0.42, Val AUC=0.80, Val ACC=0.76  ← 稳步提升
Epoch 3: Loss=0.40, Val AUC=0.82, Val ACC=0.78
...
```

**⚠️ 过拟合**：
```
Epoch 1: Loss=0.45, Val AUC=0.78
Epoch 2: Loss=0.35, Val AUC=0.83  ← 峰值
Epoch 3: Loss=0.28, Val AUC=0.80  ← 开始下降 ⚠️
Epoch 4: Loss=0.23, Val AUC=0.77  ← 继续下降 ❌
```

**❌ 训练异常**：
```
Epoch 1: Loss=nan, Val AUC=0.50   ← 梯度爆炸/消失
```

### 监控命令

```bash
# 实时查看训练日志
tail -f results/experiment_results_*.json

# 查看GPU使用情况
nvidia-smi -l 2

# 查看内存使用
watch -n 2 free -h
```

---

## 📝 实验记录模板

建议在 `参数优化记录.md` 中添加：

```markdown
## 实验 J1  （Junyi baseline - 721 concepts）

0. **运行命令**
python experiments/run_experiment.py --dataset junyi --n_runs 1

1. **数据配置**
- 知识点策略: exercise作为concept（细粒度）
- 知识点数: 721
- 训练样本: 18,148,188 条
- 验证样本: 2,592,598 条
- 测试样本: 5,185,198 条

2. **关键结果**
- 最佳 Val AUC = ? @ epoch ?
- 最佳 Val ACC = ?
- 训练时间: ? 分钟/epoch
- 总训练时间: ?

3. **结果分析**
- 与ASSIST09对比: ?
- 过拟合情况: ?
- 收敛速度: ?

4. **优化思路**
- ...
```

---

## 🔄 其他配置方案

### 方案B：使用 Topic（粗粒度，39个概念）

如果721个概念训练太慢，可以降级到topic：

**步骤1**：修改 `download_with_edudata.py`
```python
'junyi': {
    'user_id': 'student_id',
    'exercise': 'question_id',
    'topic': 'concept_id',        # 改为 topic
    'correct': 'correct',
    'time_done': 'timestamp',
}
```

**步骤2**：需要添加特殊处理逻辑
```python
# 在 convert_to_our_format 函数中添加
if dataset_name == 'junyi' and 'topic' not in df.columns:
    # 从 Exercise_table 获取 topic
    exercise_table = pd.read_csv('junyi/junyi_Exercise_table.csv')
    df = df.merge(exercise_table[['name', 'topic']], 
                  left_on='exercise', right_on='name', how='left')
```

**优点**：训练速度快（概念少）  
**缺点**：粒度粗，可能损失精度

---

## 📚 数据文件说明

### `junyi_ProblemLog_original.csv` (主数据)

| 列名 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `user_id` | int | 学生ID | 12345 |
| `exercise` | str | 练习名称 | solving_linear_equations_1 |
| `problem_type` | str | 题目模板 | - |
| `problem_number` | int | 第几次练习 | 1, 2, 3... |
| `correct` | bool | 是否正确 | True/False |
| `time_done` | int | 答题时间戳 | 1404825600 |
| `time_taken` | int | 耗时（秒） | 45 |
| `count_attempts` | int | 尝试次数 | 1 |
| `hint_used` | bool | 是否使用提示 | True/False |
| `earned_proficiency` | bool | 是否达到熟练 | True/False |

### `junyi_Exercise_table.csv` (元信息)

包含每个exercise的：
- `name`: 练习名称（ID）
- `topic`: 所属主题（39个）
- `area`: 所属领域
- `prerequisite`: 前置练习
- `pretty_display_name`: 中文显示名称

### `relationship_annotation_*.csv` (关系标注)

专家标注的练习间关系（可用于构建更精确的知识图谱）：
- `Similarity_avg`: 相似度
- `Difficulty_avg`: 难度差异
- `Prerequisite_avg`: 前置关系

---

## ✅ 验证清单

实验前检查：
- [ ] 数据已下载到 `junyi/` 文件夹
- [ ] 转换后的 `data/junyi.csv` 存在
- [ ] 知识点数为 **721**（exercise级别）
- [ ] pkl文件已生成（`data/processed_datasets.pkl`）
- [ ] GPU内存 ≥ 8GB
- [ ] 系统内存 ≥ 16GB
- [ ] 磁盘剩余空间 ≥ 15GB

实验中监控：
- [ ] 第1个epoch能正常完成
- [ ] Loss持续下降
- [ ] AUC在合理范围（0.75-0.85）
- [ ] 内存使用稳定
- [ ] 没有严重过拟合

---

## 📞 技术支持

遇到问题？检查：
1. ✅ 查看本文档的"常见问题"章节
2. ✅ 参考 `参数优化记录.md` 中的优化经验
3. ✅ 检查 `README_实验运行指南.md`
4. ✅ 查看终端错误信息和日志

---

**最后更新**：2025-12-29  
**配置版本**：Exercise作为Concept（721个，细粒度）

