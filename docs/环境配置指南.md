# 环境配置指南

## 🐍 Python环境

### 系统要求
- Python 3.9+
- CUDA 11.8+ (推荐使用GPU)
- 16GB+ RAM (ASSIST09最低)
- 32GB+ RAM (Junyi推荐)

## 📦 快速安装

### 1. 创建conda环境
```bash
conda create -n krd-kt python=3.9
conda activate krd-kt
```

### 2. 安装PyTorch（GPU版）
```bash
# CUDA 11.8
pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu118

# CUDA 12.1
pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu121
```

### 3. 安装PyTorch（CPU版）
```bash
pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cpu
```

### 4. 安装项目依赖
```bash
pip install -r requirements.txt
```

## 🔍 验证安装

```bash
# 验证PyTorch
python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA available: {torch.cuda.is_available()}')"

# 验证项目依赖
python -c "import numpy, pandas, sklearn, tqdm; print('All dependencies OK!')"
```

### 预期输出
```
PyTorch: 2.1.0
CUDA available: True
All dependencies OK!
```

## 📋 requirements.txt

```txt
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=0.24.0
tqdm>=4.62.0
matplotlib>=3.4.0
seaborn>=0.11.0
```

## ⚙️ 可选工具

### Jupyter Notebook（用于数据分析）
```bash
pip install jupyter
jupyter notebook
```

### TensorBoard（用于训练可视化）
```bash
pip install tensorboard
tensorboard --logdir=runs
```

## 🐛 常见问题

### Q1: CUDA不可用？
**检查**:
```bash
nvidia-smi  # 查看CUDA版本
```

**解决**:
- 确保安装了对应版本的PyTorch
- 检查NVIDIA驱动是否正确安装

### Q2: 内存不足？
**解决**:
- 减小batch_size
- 减小max_seq_len
- 使用CPU（速度较慢但可行）

### Q3: 依赖冲突？
**解决**:
```bash
# 重新创建环境
conda remove -n krd-kt --all
conda create -n krd-kt python=3.9
```

## 🚀 性能优化

### GPU加速
- 使用CUDA（必须）
- 增大batch_size
- 启用混合精度训练（可选）

### 多进程数据加载
```python
# 在Windows上设置num_workers=0
# 在Linux/Mac上可以设置num_workers=4
```

## 📊 资源需求

| 数据集 | GPU内存 | 系统内存 | 训练时间 |
|--------|---------|----------|----------|
| ASSIST09 | 4GB | 16GB | 1-2小时/run |
| Junyi | 8GB | 32GB | 10-20小时/run |
| EdNet | 16GB | 64GB | 50-100小时/run |

---

**最后更新**: 2026-02-06

